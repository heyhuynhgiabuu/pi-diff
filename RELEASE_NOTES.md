# @heyhuynhgiabuu/pi-diff v0.8.2

## Fixed

- **Tool rendering spacing** — normalize `write`, `edit`, and `apply_patch` previews around Pi's default tool shell: one host-provided leading space, no redundant edge padding, one title/body separator, and one trailing shell pad.
- **Pi SDK alignment** — update the direct Pi SDK dependencies to **0.85.0**.
- **Renderer regression coverage** — add tests for tool titles, create previews, diff bodies, non-diff results, and errors.

## Install

```bash
pi install npm:@heyhuynhgiabuu/pi-diff@0.8.2
```
